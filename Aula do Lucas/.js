let infoe = document.getElementById("email")
let infos = document.getElementById("senha")

function exc() {
    window.confirm(email.value)
    window.confirm(senha.value)
    let res = window.prompt('Digite seu Nome de usuário')
    window.alert(`Bem Vindo ${res}`)
}
